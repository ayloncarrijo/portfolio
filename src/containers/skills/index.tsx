import { Section } from "@/components/section";
import { SectionTitle } from "@/components/section/section-title";
import { Container } from "@vista-ui/react";

export const Skills = (): JSX.Element => {
  return (
    <Section id="skills">
      <Container>
        <SectionTitle>Habilidades</SectionTitle>
        <div>...</div>
      </Container>
    </Section>
  );
};
